name: riverpod_navigator_2_0
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_riverpod: ^1.0.0-dev.6
  freezed_annotation: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed: 
  pedantic:

flutter:
  uses-material-design: true
